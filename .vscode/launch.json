{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug pipex",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/a.out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "externalConsole": false,
            "preLaunchTask": "Build pipex"
        }
    ]
}

