{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug pipex",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/pipex",
            "args": ["ping -c 5 google.com", "grep rtt"],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "externalConsole": false,
            "preLaunchTask": "Build pipex"
        }
    ]
}

